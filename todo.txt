uzycie notification model dla sledzenia stanu wyslania email 
i/lub do potwierdzenia rejestracji bardziej przez email i dla emaili co najwyzej 2 stany
wyslane, i wysylany, i nie wyslany tylko nie wiem czy jest to mozliwy stan?

zrobic logike dla  bookbmark

ressiliance np retry,circut breaker dla frontu w angular

zamiast local storage to lepiej http cookies i same site strict ale to potem

refresh tokeny z jwt zrobic moje
zrobic revoked dla refersh token jesli jest expired

dla github actions testy jednostkowe back i front dorobic

uzycie rate limit i innych zabezpieczen w controller rate limity dorobic moze na ip lub jwt lepiej ale mozna 
chyba tez na ip?

te co uzytkownik ma kupiony bilet to nie moze kupic 2 raz tego samego moze zeby nie byl widoczny albo byl widoczny
tylko jakies inne id by nie wywalil exception w bazie?

uzycie when.all z tpl (parallel library) jesli 1 rzecz nie jest zalezna od drugiej dzieki temu szybko sie wykona
akcja

admin pannel przeglądania list uczestników, seed bazy podglądu statystyk (liczba zapisów, frekwencja),
admina tworze przy migracji jesli moge przy migracji hashowac haslo

uzycie observer do serwisu z sms i email?

zrobienie Single Responsibility Principle np dla backendu dla zcytywajnia emailu z jwt

caching np 
app.UseOutputCache();
[HttpGet]
[OutputCache(Duration = 60)]

healthcheck
builder.Services.AddHealthChecks();
app.MapHealthChecks("/healthz");
https://learn.microsoft.com/en-us/aspnet/core/host-and-deploy/health-checks?view=aspnetcore-10.0

dla pojedynczych getow powinienem dawac ze parametr moze byc null ipotem not found

ASP.NET Core routing bo constraint opisuje kontrakt URL-a już na poziomie routingu.
https://learn.microsoft.com/en-us/aspnet/core/fundamentals/routing?view=aspnetcore-10.0
[HttpGet("{id:int:min(1)}")]        // znaczy ze od 1 zaczyna sie a nie ze 1 liczba
public async Task<ActionResult<int>> GetName(int id)
[HttpGet("{id:noZeroes}")]
public class NoZeroesRouteConstraint : IRouteConstraint{private static readonly Regex _regex = new()}
if (id.Contains('0')){return StatusCode(StatusCodes.Status406NotAcceptable);} ale tez slabe to jest
bo nie moze byc 0 ale takze nie moze byc 10,100...

AllowedHosts / reverse proxy > [Host] z konfiguracją.

app.MapGet("/short-circuit", () => "Short circuiting!").ShortCircuit();

builder.Services.AddProblemDetails();
app.UseExceptionHandler("/Error")   //dla 500
https://learn.microsoft.com/en-us/aspnet/core/fundamentals/error-handling?view=aspnetcore-10.0

app.UseStatusCodePages(async context => {}) // to jest jesli wpisze np https://www.youtube.com/asd i mam 
not found

[BindNever]
jesli tylko aplikacja moze nadac user miec pole isadmina 
 nie jest to dobre podejscie ale sie da

dla takich abstrakcji warto korzystac z repozytorium i bardziej zaawansowanych
public interface IOrderRepository
{
    Order? GetOrderWithDetails(int id);
    IEnumerable<Order> GetUnshippedOrders();
}
 public Order? GetOrderWithDetails(int id)
{
    return context.Orders
        .Include(o => o.Lines)
        .ThenInclude(l => l.Product)
        .FirstOrDefault(o => o.OrderId == id);
}

powinienem dawac id uzytkownika w url czesciej mysle
app.MapGet("/users/{userId}/books/{bookId}")

powinienem przy tworzeniu zamiast dawac ok 200 to 
CreatedAtAction(nameof(GetProduct), new { id = product.Id }, product) 201
204 No Content Operacja zakończona sukcesem, ale nie zwracasz treści
400 Bad Request Nieprawidłowe dane wejściowe
409 Conflict Konflikt, np. próba utworzenia zasobu, który już istnieje 

korzystac z repozytoriow raczej jesli pracuje z duza ilosci danych jesli to sa proste zapytania to mozna
raczej baze? bo niewiele to zmieni wsm

uzytkownik jak trafi na strone z authorize a nie jest to redirect na login od razu
builder.Services.AddAuthentication(CookieAuthenticationDefaults.AuthenticationScheme)
    .AddCookie(options => 
    {
        options.LoginPath = "/login";
    }); 

https://dotnettutorials.net/lesson/token-based-authentication-web-api/?redirectUrl=https%3A%2F%2Flearn.epam.com%2FmyLearning%2Fpath%3FrootId%3D46936858

Nie rób throw ex Złe: catch (Exception ex) { throw ex; } 
Lepsze: catch (Exception) { throw; }
bo trace stack (informacje trace)

[Flags] 
enum Permissions { None = 0, Read = 1, Write = 2, Delete = 4 }
Dzięki temu możemy je łączyć: (bo sa bitowo)
Dlaczego wartości są 1, 2, 4, 8? Ponieważ są potęgami 2: 1 = 001 2 = 010 4 = 100 Dzięki temu można je 
łączyć bitowo

moze sie przydac subscribe i event?
public class OrderService
{
    public event EventHandler? OrderCreated;

    public void CreateOrder()
    {
        Console.WriteLine("Order created");

        OrderCreated?.Invoke(this, EventArgs.Empty);
    }
}
Subskrypcja:
OrderService service = new OrderService();
service.OrderCreated += OnOrderCreated;
static void OnOrderCreated(
    object? sender,
    EventArgs e)
{
    Console.WriteLine("A new order was created");
}
Jeżeli event ma przekazywać dane, tworzymy klasę dziedziczącą po EventArgs.

publisher żyje dłużej niż subscriber, subscriber jest tymczasowy,
prawidlowo
public class TemporaryWindow : IDisposable { 
private readonly LongLivedService _service; 
public TemporaryWindow(LongLivedService service) { _service = service; 
_service.SomethingHappened += OnSomethingHappened; } 
private void OnSomethingHappened( object? sender, EventArgs e) { } 
public void Dispose() { _service.SomethingHappened -= OnSomethingHappened; } }
Użycie: using (var window = new TemporaryWindow(service)) { // Praca z obiektem }

i mysle ze dla mamy muslial bym jesli by cos chciala to robic kolejke

by dobrze zrobic di trzeba interface dla service